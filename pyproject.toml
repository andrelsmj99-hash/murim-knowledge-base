[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "W",   # pycodestyle warnings
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "SIM", # flake8-simplify
]
ignore = [
    "E501", # line too long (handled by ruff-format)
    "B008", # do not perform function calls in argument defaults
    "B904", # raise-without-from-inside-except
]
fixable = ["ALL"]
unfixable = []

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["N806", "N802", "N803"]
"app/models/*" = ["N801"]
"alembic/**/*" = ["N801", "N802", "N806"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.pytest.ini_options]
markers = [
    "e2e: end-to-end tests requiring browser (Playwright)",
]
testpaths = ["tests"]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
check_untyped_defs = true
no_implicit_optional = true
warn_return_any = true
warn_unused_ignores = true
disallow_untyped_defs = false
exclude = ["alembic/"]

[[tool.mypy.overrides]]
module = [
    "spacy.*",
    "sentence_transformers.*",
    "streamlit.*",
    "plotly.*",
    "networkx.*",
    "rapidfuzz.*",
    "tenacity.*",
    "beautifulsoup4.*",
    "lxml.*",
    "transformers.*",
]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = [
    "app.repositories.*",
]
# SQLAlchemy 2.x ORM Column types are inherently incompatible with mypy
# when used in constructors and assignments — false positives.
disable_error_code = ["arg-type", "assignment", "union-attr", "call-arg", "attr-defined", "valid-type", "var-annotated", "no-any-return"]